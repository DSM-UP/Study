const arr = ["a", "b", "c"];
arr.length; // 3

const arr = ["a", "b", "c"];

// 첫번째 요소를 가져옵니다.
arr[0];
// arr의 마지막 요소의 인덱스는 arr.length-1입니다.
arr[arr.length - 1]; // 'c'

const arr = [1, 2, "c", 3, 4];
arr[2] = 3; // arr은 이제 [1, 2, 3, 4, 5]입니다.
